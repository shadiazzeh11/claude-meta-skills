Note: This file is the global verification-protocol backstop. It is intentionally minimal and protocol-focused, not project documentation. Tools that audit CLAUDE.md against project-quality rubrics should treat this file as out-of-scope or apply rubric criteria that match its actual purpose (protocol minimality, not project completeness).

Verification protocol. Before recommending any state-changing action (install, marketplace add, file write, config change, command execution) or making any factual claim about external tools, repos, or systems, invoke the verification-before-recommend skill. The skill enforces: verify the command/claim resolves to the stated behavior with a citation or explicit "unverified" label; check current state before adding new state; distinguish verified facts from inferences in writing; propose the smallest reversible test that would invalidate the plan if wrong; resolve disagreements by verifying the load-bearing claim, not by additional rounds of pushback.
