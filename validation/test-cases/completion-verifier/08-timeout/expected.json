{
  "expected_exit_code": 0,
  "expected_stdout_contains": ["systemMessage", "timed out", "Partial output before timeout", "started before timeout"],
  "expected_stdout_not_contains": ["additionalContext"],
  "description": "Long-running test (sleep 5) with COMPLETION_VERIFIER_TIMEOUT_SECS=2 must warn with partial output, not block",
  "category": "should-pass",
  "env": {
    "COMPLETION_VERIFIER_TIMEOUT_SECS": "2"
  }
}
