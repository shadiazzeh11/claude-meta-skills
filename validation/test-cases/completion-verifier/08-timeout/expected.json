{
  "expected_exit_code": 0,
  "expected_stdout_contains": ["additionalContext", "timed out"],
  "description": "Long-running test (sleep 5) with COMPLETION_VERIFIER_TIMEOUT_SECS=2 must produce timeout warning, not block",
  "category": "should-pass",
  "env": {
    "COMPLETION_VERIFIER_TIMEOUT_SECS": "2"
  }
}
