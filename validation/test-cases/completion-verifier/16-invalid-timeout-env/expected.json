{
  "expected_exit_code": 0,
  "expected_stdout_contains": ["decision", "block", "Tests are failing"],
  "expected_stdout_not_contains": ["additionalContext", "timed out"],
  "expected_stderr_empty": true,
  "description": "Invalid COMPLETION_VERIFIER_TIMEOUT_SECS falls back to default and still blocks on failing tests",
  "category": "should-block",
  "env": {
    "COMPLETION_VERIFIER_TIMEOUT_SECS": "abc"
  }
}
