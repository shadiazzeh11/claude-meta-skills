{
  "expected_exit_code": 2,
  "expected_stderr_contains": ["protected pattern", "poetry"],
  "description": "Write to poetry.lock → blocked (matches poetry\\.lock$ pattern)",
  "category": "should-block"
}
