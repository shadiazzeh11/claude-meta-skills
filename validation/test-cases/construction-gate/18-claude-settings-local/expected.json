{
  "expected_exit_code": 2,
  "expected_stderr_contains": ["protected pattern", "local"],
  "description": "MultiEdit on .claude/settings.local.json → blocked (MultiEdit tool with file_path; matches .claude/settings\\.local\\.json$ pattern)",
  "category": "should-block"
}
