{
  "expected_exit_code": 0,
  "expected_stdout_contains": [],
  "expected_stdout_empty": true,
  "description": "Relative notebook_path resolved against payload cwd; notebook exists; hook silently allows (no missing-file warning)",
  "category": "should-pass"
}
